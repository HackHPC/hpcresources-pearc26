source "https://rubygems.org"

# Pins Jekyll + plugin versions to whatever GitHub Pages actually runs in
# production, so `bundle exec jekyll serve` matches the deployed build.
gem "github-pages", group: :jekyll_plugins
